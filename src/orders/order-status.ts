export enum OrderStatus {
  Pending = 'pending',
  PaymentPending = 'payment_pending',
  Paid = 'paid',
  Preparing = 'preparing',
  Shipped = 'shipped',
  Cancelled = 'cancelled',
  Failed = 'failed',
}
